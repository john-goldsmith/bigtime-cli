let dateRange = require('./date-range'),
    dateRangeSummary = require('./date-range-summary'),
    detail = require('./detail'),
    create = require('./create'),
    destroy = require('./destroy'),
    update = require('./update');

module.exports = {
  dateRange,
  dateRangeSummary,
  detail,
  create,
  destroy,
  update
};